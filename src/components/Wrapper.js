import React from "react";
import Header from "./Header";

const Wrapper = props => {
    return(
        <div className="container">
            <Header />
        </div>
    );
};

export default Wrapper;